//! The `wickra-screener` reference CLI.
//!
//! Loads a `ScanSpec` and a directory of per-symbol CSV candle files, runs a
//! scan through `screener-core`, and prints the report as text or JSON. The
//! argument parsing, run logic and glue are filled in over P-SCR-2.2 to 2.4.

fn main() {}
